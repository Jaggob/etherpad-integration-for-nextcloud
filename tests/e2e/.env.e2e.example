# SPDX-License-Identifier: AGPL-3.0-or-later
#
# Copy this file to tests/e2e/.env.e2e (gitignored) and fill in the
# values for YOUR test Nextcloud instance. Never commit real credentials.
#
# Use a dedicated throwaway test account, not your daily-driver user —
# the E2E specs create and delete .pad files on this instance.

# Base URL of the Nextcloud instance under test (no trailing slash).
E2E_BASE_URL=https://cloud.example.test

# Test-account login (used once by auth.setup.ts to produce a browser
# session; password is never stored in the repo).
E2E_USER=e2e-tester
E2E_PASS=change-me

# App password for the same account, used for non-browser WebDAV
# teardown (deleting pads the specs created). Create one under
# Settings → Security → Devices & sessions. Optional but recommended —
# without it, specs leave their test files behind.
E2E_APP_PASSWORD=
